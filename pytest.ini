[pytest]
testpaths = tests
addopts = -ra
markers =
    footage: needs the private test plates (CONTOUR_FOOTAGE); never copy them into the repo
    models: loads model weights from models/ (slow, CPU is fine)
    gpu: needs a CUDA GPU
xfail_strict = true
